#!/usr/bin/env python3
"""Minimal runnable code listed by the generic paper template."""


def model(x: float, theta: float) -> float:
    return theta * x


if __name__ == "__main__":
    print(model(2.0, 3.0))
