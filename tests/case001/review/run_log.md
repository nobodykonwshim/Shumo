# case001 实战运行日志

## 2026-07-12：初始化与 W0/W1 预运行

1. 从三层架构 PR 创建 `test/case001-multibeam-pilot`。
2. 读取 RAR 目录并记录四个原始文件。
3. 登记三篇参考论文的标题、页数、哈希、方法和结果分歧。
4. 建立项目配置、验收标准、W0、推断版 W1、G1 和缺陷日志。
5. 因运行环境不能解压 RAR，Workflow 正确停在 W1，不依据参考论文直接开算。

初始停止点：

```text
W0 partial
→ W1 inferred_pending_original_verification
→ G1 preliminary
→ STOP
```

本轮首先证明：源文件可读性属于 W0 质量门槛，不能交给 Agent 猜测或绕过。

## 2026-07-12：原始文件补充与问题一、二 W2-W4

### 源文件核验

用户分别提供了 `B题.pdf`、`附件.xlsx`、`result1.xlsx` 和 `result2.xlsx`。完成：

- 原题面三页逐问核验；
- 四个文件 SHA-256 登记；
- 两个结果模板字段和范围核验；
- 附件 251×201 深度网格检查；
- 坐标范围 4×5 海里、步长 0.02 海里、缺失值为 0；
- 水深范围 20.0–197.2 m。

W0 更新为 `complete`，W1 更新为 `verified`。问题四仍未允许调用 Agent，因为独立空间覆盖评价器尚不存在。

### 问题一

采用确定性几何 Workflow：

1. 规定横向坐标正方向指向浅水侧；
2. 建立水深、深水侧半覆宽、浅水侧半覆宽和总覆宽；
3. 将“与前一条测线重叠率”定义为重叠长度除以当前条带总宽；
4. 检查零坡度极限、量纲、单调性和漏测边界；
5. 填写原 `result1.xlsx` 模板并增加 Calculation 审计页。

关键结果：首个负重叠率出现在偏移 600 m，说明浅水侧最后两段开始漏测。

### 问题二

采用确定性三维几何 Workflow：

1. 沿测线方向的深度变化由坡度梯度在航向上的投影确定；
2. 横测面有效坡度由坡度梯度在测线法向上的投影确定；
3. 使用问题一覆宽几何计算 8×8 表格；
4. 通过 45°/315°、135°/225° 对称性和 90°/270° 常值性验证；
5. 注入“把 0.3 海里当成 0.3 米”的单位错误，验证器成功通过数量级检查发现异常。

发现一项参考论文异常：`beta=135°、距离=2.1 海里` 的独立结果为 `166.430 m`，且必须与 225° 对称；部分参考材料中的 `116.430 m` 被标记为疑似排版或转录错误。

## 2026-07-12：问题三参考隔离与人工检查点

### 发现的实验设计问题

在问题三独立模型冻结之前，当前对话和仓库已经读取了三篇参考论文，并知道：

- 常见路线是沿等深线布线；
- 三篇论文均报告 34 条测线；
- 常见总长度为 68 海里。

因此，当前 case001 不能再诚实地称为“未知题目盲态独立建模”。仅要求执行者忽略已经看过的信息，不能消除锚定效应。

### 已采取的处理

1. 新增通用 `REFERENCE_ISOLATION_PROTOCOL.md`；
2. 建立 `review/reference_exposure_log.yaml`；
3. 将当前状态标记为 `contaminated_for_blind_claim`；
4. 用户批准采用“两阶段流程”：先独立建模，再开放参考对照；
5. 问题三第一阶段改为确定性 Workflow，禁止 Route Explorer、参考论文读取和同题联网检索；
6. 第一阶段只能读取原题面、问题一和问题二的已验证成果；
7. 独立成果必须冻结并记录哈希后，才能经新的人工检查点开放参考资料。

由于已有暴露，问题三第一阶段被准确命名为：

```text
reference-quarantined reconstruction
```

它可以测试 Workflow、环境权限和来源追踪，但不能证明真正的盲态原创性。

### 当前状态

```text
W0 complete
→ W1 verified
→ G1 verified
→ problem1 W2-W4 pass
→ problem2 W2-W4 pass
→ H1 problem3 two-phase process approved
→ problem3 W2 ready under reference quarantine
→ problem4 blocked pending independent evaluator
```

### 新的框架反馈

1. Workflow-first 路由有效：问题一、二无需 Agent 即可稳定完成。
2. 原始输入阻塞和参考论文异常均被显式记录，没有被静默掩盖。
3. 参考资料必须在 W0 分类，并通过文件权限和环境清单实现真正隔离，不能只靠提示词说“不要参考”。
4. 最大剩余工程缺陷仍是执行层人工编排：尚无 schema validator、reference-access gate、stage runner 和自动回滚。
5. 真正检验未知题目能力，需要使用一个从未向执行环境暴露参考方案的 `case002` 或全新隔离会话。